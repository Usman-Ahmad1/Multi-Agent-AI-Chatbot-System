# Force pre-built wheels
--only-binary=:all:
Pillow==10.4.0

# Core dependencies
groq==0.9.0
python-dotenv==1.2.2
requests==2.32.3
beautifulsoup4==4.12.3

# UI
streamlit==1.38.0   # Slightly newer version (more stable)

# Data & Utils
pandas==2.2.2
Pillow==10.4.0      # Keep but we'll force wheel

# Optional but useful
langchain==0.3.0
langgraph==0.2.0
langchain-community==0.3.0